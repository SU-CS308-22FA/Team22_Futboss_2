import React, {Component, useEffect, useState} from 'react';
import { Link, useNavigate } from 'react-router-dom';
import Axios from 'axios'

export default function ProfilePage () {
    window.scrollTo(0, 0)
    return(
        <Link to="/"><button>Delete User</button></Link>
    )
}